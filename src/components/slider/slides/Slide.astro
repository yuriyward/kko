---
const { class: className, ...rest } = Astro.props;
---

<div
    class:list={[
        "rounded-3xl flex p-12 flex-col gap-10 lg:flex-row lg:gap-20 h-full",
        className,
    ]}
    {...rest}
>
    <slot />
</div>
